# WP3 / WP3-A — The or- System (L2 slate, H1–H4)

*Builder round 2026-09-19; adversary round (Smaug) same night. Inputs inherited frozen from
WP2; nperm=1000, seed 20260919, crash-resume verified bit-identical.*

## Design intent

Lucen's L2 slate, informed by WP2's survivors: H1 terminal appendage (linguistic particle
class), H2 accumulative residue (process decay), H3 semantic anchor/coda (block closure),
H4 layered transcription (gloss layer) — with a preregistered **master axis** (T2):
linguistic mechanisms predict context selectivity and stacking-avoidance; process mechanisms
predict neighbor statistics ≈ background. Battery T1–T5, both transliterations.

## Master verdict

**PROCESS-leaning — the linguistic-integration signature is absent.** Left-neighbor entropy
of or- contexts is at or above the stratum-matched null (lower-tail p = .905 ZL / .973 IT);
no particle-stacking avoidance, no gloss runs. Composite profile:

> or- words look like insertions drawn from a small closed repertoire built out of the
> corpus's own common stems, preferentially appended at line ends as a step (not a ramp),
> without selective syntactic context, without label-style verbatim repetition, and without
> block-closure geometry.

Per-mechanism: **H1 SUPPORTED on prereg letter** (T1a TTR .145/.147 vs .653/.661 controls,
p=.001 both; T1b stem attestation .788/.739, p=.001 both) with a master-axis asterisk;
**H2 prereg-letter supported, robustness MIXED** (the T4a excess is symmetric — most of it
is substring-commonness, not derivation); **H3 DISFAVORED** on all three prongs (terminal
enrichment does NOT exceed line-final, p=.71/.63; over-dispersed counts; gradient weakens in
bigger blocks); **H4 MIXED** (the line-final STEP is its predicted shape — final−penultimate
+.0078/+.0075, p=.004/.006, penultimate ≈ medial — but repetition, runs, and ink coupling
all fail).

## Adversary outcome (WP3-A)

- **Fingerprint + recompute of every published cell: no fabrication anywhere; deviations are
  convention-level and the direction of evidence never flips.** (Method self-correction
  recorded: fingerprint tolerance must match printed precision — half-ULP of the as-printed
  decimals; WP2's fabricated C7 cells still fail under the corrected rule.)
- **H1 DOWNGRADED:** both legs are real, but the controls measure generic
  prefix-familyhood. Against the honest comparator universe (all 2-char prefix families
  ≥300 tokens), or- ranks #7/24 (ZL) and #4/22 (IT) on TTR; #11/60 and #13/63 on stem
  attestation. Reproducible content: *or- behaves like an ordinary productive EVA prefix
  family; particle status not established.*
- **H2's surviving directional residue KILLED:** the ZL-only up−down asymmetry (p=.003) is a
  forking path — or- residuals skew short; adding residual-length matching to the control
  pool moves the null from −.032 to +.011 and p from .025 to **.46**. Both transliterations
  null under the matched control. What stands is only the symmetric fact.
- **Master axis REPLICATED** with the adversary's own estimator (Miller–Madow), null, and
  seed: integration absent (strong, both translits); *active* anti-selectivity is IT-only
  moderate (p=.028–.037; ZL trend .06–.096). Accepted as campaign fact with that caveat.
- **The STEP independently replicated** (p=.005/.010) and sharpened: paragraph-terminal ∩
  line-final is the hottest positional cell measured (.0336/.0398); the line-final effect
  survives even inside paragraph openings (.0195/.0211 vs corpus mean .0154/.0161) — one
  end-position grammar at two scales, multiplying.

## Key numbers

| Result | ZL | IT |
|---|---|---|
| T1a or- TTR vs controls | .145 vs .653, p=.001 | .147 vs .661, p=.001 |
| T1b stem attestation vs null | .788 vs .493, p=.001 | .739 vs .484, p=.001 |
| T2a left-entropy lower tail (integration) | p=.905 | p=.973 |
| T5b final − penultimate (STEP) | +.0078, p=.004 | +.0075, p=.006 |
| H2 directional residue after length matching (adversary) | p=.46 | already null (.39) |
